# P0 改造交付说明（Phase 1–3）

## 这个包是什么

Silver Test Platform 的工作副本，包含 P0 改造的前三个阶段：

| 阶段 | 内容 | 状态 |
|---|---|---|
| Phase 1 | 设计系统统一（三份 CSS 去重、单一真源） | ✅ |
| Phase 2 | 统一确认/输入弹窗（消灭 19 处原生 `confirm`/`prompt`） | ✅ |
| Phase 3 | 深链接（URL 承载视图状态） | ✅ |
| Phase 4 | 工作台 + 跨项目任务视图 | ⬜ 未开始 |
| Phase 5 | 导航语义与面包屑 | ⬜ 未开始 |

详细决策记录见 `_review/PLAN.md`。

---

## 打包时排除了什么

- `__pycache__/` / `*.pyc`
- **`frontend/node_modules/`** —— 这是 6 个符号链接，指向你给的那 4 个 lib 压缩包
  解出来的 223M（`@univerjs` 175M、`@univerjs-pro` 42M、`yjs`/`y-*`）。
  内容和你手上的原包一字不差，不重复传输。恢复方式见下。
- **`.env`** —— 它在 `.gitignore` 里，而且装着真实凭据：
  `SECRET_KEY`、`LM_ADMIN_PASSWORD`、PostgreSQL DSN。不该出现在传来传去的压缩包里。
  `.env.example` 已保留，**你需要用自己原来的 `.env` 覆盖回去**（我没有改过它）。

其余**全部**原样保留，未做任何裁剪。

---

## 怎么恢复 node_modules

```bash
cd frontend && mkdir -p node_modules && cd node_modules
# 把 4 个 lib zip 解到某处，然后：
ln -s <解压路径>/@univerjs      @univerjs
ln -s <解压路径>/@univerjs-pro  @univerjs-pro
ln -s <解压路径>/yjs            yjs
ln -s <解压路径>/y-protocols    y-protocols
ln -s <解压路径>/y-websocket    y-websocket
ln -s <解压路径>/y-leveldb      y-leveldb
```

注意：**前端 Univer/协同编辑那部分我没有重新构建过**（沙箱里没有 npm/vite）。
这些是渐进增强模块，不影响本轮改的任何页面，但你那边如果有构建流程，
建议跑一次确认没被我的改动影响。

---

## 怎么验证

### 1. 不需要数据库的检查（可直接跑）

```bash
cd _review

python3 render_check.py    # 15 个模板全部渲染 → 期望 "15 templates, 0 failures"
python3 verify.py          # CSS 回归 → 期望 "RESULT: PASS"
python3 tplcheck.py        # 模板结构 → 期望 "RESULT: PASS"
python3 phase4_check.py    # 工作台接线 → 期望 "RESULT: PASS"
python3 phase5_check.py    # 面包屑 / 弹窗合并 / CSS 编号 → 期望 "RESULT: PASS"
node    test_ui.js         # 弹窗组件 → 期望 "51 passed, 0 failed"
node    test_urlstate.js   # 深链接   → 期望 "36 passed, 0 failed"
node    test_home.js       # 工作台   → 期望 "81 passed, 0 failed"
node    test_crumb.js      # 面包屑   → 期望 "36 passed"
```

`render_check.py` 从 13 涨到 15，是因为新增了 `_crumb.html` 与 `_steps_dialog.html` 两个 include。

`phase5_check.py` 检查的是**渲染结果**而不只是「能渲染」——include 解析成空字符串
时模板依然渲染成功，只有断言产出的 HTML 才能发现这种情况。

### 1b. 变异测试（验证「测试自己」是否有效）

测试全绿不代表测到了东西。下面四个脚本会**逐条破坏源码**，
要求对应测试必须变红；有任何一条 `MISSED` 就说明该测试是摆设。

```bash
python3 phase4_mutate.py      # me.py 的 5 个安全守卫 → 全部 CAUGHT
python3 phase4_mutate_js.py   # home.js 的 10 个行为   → 全部 CAUGHT
python3 mutate_all.py         # ui.js 7 个 + urlstate.js 5 个 → 全部 CAUGHT
python3 mutate_crumb.py       # crumb.js 的 13 个行为  → 全部 CAUGHT
```

脚本会自动备份并还原源码；最后一行会重跑一次原始测试确认已还原干净。

**变异测试确实抓出了真实问题**（详见 PLAN.md 各阶段末节）：
- `test_home.js` 原先只测了「先选状态→再勾 mine」，漏了反方向；
  若互斥逻辑损坏，工作台会显示一个悄悄漏掉同事运行的「运行中」计数。
- `crumb.js` 的 `apply()` 里有一个**永远不可达**的空值分支（两个调用点都已 guard），已删除。

同样值得说明的是：`mutate_crumb.py` 首轮 6 条 MISSED 里有 5 条是**变异写错了**，不是用例漏测
（等价变异、自我抵消的变异）。**一条 MISSED 不必然意味着测试有问题，先复查变异本身**，
否则会为了让变异变红而写出错误的断言。

`render_check.py` 绕开 Flask 和 PostgreSQL 直接渲染模板——因为 `create_app()`
启动时会跑一次建表迁移，没有 PG 就起不来。你那边有 PG 的话可以忽略这个绕道。

### 2. JS 语法

```bash
cd app/static/js/lanmatrix && for f in *.js; do node --check "$f"; done
```

### 3. 后端测试

```bash
python3 -m pytest -q
```

**我这边的基线：`1 failed, 156 passed, 3 skipped, 20 errors`。**

- 那 **1 个 failed 是改造前就存在的**，与本轮无关：
  `tests/test_collab_awareness.py::RowActorsTest::test_cursor_wins_over_selection`
- 那 **20 个 error 全是 connection refused**——沙箱里没有 PostgreSQL。
  **你那边接上 PG 后这 20 个才是真正的验证重点，我无法覆盖。**

---

## 需要人工点一遍的地方（我跑不了浏览器）

自动化测试覆盖了逻辑，但以下要真人在浏览器里确认：

**深链接（Phase 3）**
1. 任务列表筛选一下（状态/提交者/搜索），看地址栏出现 `?status=&q=`；
   **复制 URL 到新标签页打开，筛选应当原样还原**。
2. 打开某个任务详情 → URL 出现 `?task=<key>`；**刷新页面应当直接停在该任务**。
3. 在任务详情里按「后退」→ 回到列表，**且之前的筛选还在**。
4. 直接用 `?task=X` 打开（不经过列表），再点关闭 → 应当回到列表，
   **不应该跳出站外**。
5. 管理后台 / 数据库页切 Tab → `?tab=`；刷新停在原 Tab。
6. 默认视图 URL 应当是干净的（`/admin` 而不是 `/admin?tab=users`）。

**弹窗（Phase 2）**
7. 删除项目/字段/用户等「critical」操作，需要重新输入标识符才能点确认。
8. **重点**：在 SBS 编辑器里按 Esc 触发关闭确认，
   **Esc 应当只关掉确认框，不应该连编辑器一起关掉**（这是我修的一个真实 bug）。
9. 危险操作的弹窗默认焦点应在「取消」上，误按回车不会删东西。

**样式（Phase 1）**
10. 失败步骤在日志里应当高亮成红色（这也是修的一个真实 bug——
    之前的选择器祖先永远匹配不上，失败步骤从来没高亮过）。
11. 深色模式下弹窗遮罩、窄屏下弹窗按钮堆叠。

**工作台（Phase 4）— 本轮新增，请重点看这一段**
12. **登录后应当落在「工作台」而不是项目列表**（这是一处刻意的行为变更，
    如果你们团队习惯了直接进项目列表，这条要先确认能不能接受）。
13. 点 KPI 磁贴（如「失败」）→ 下方任务列表被筛选，磁贴高亮；
    **再点一次同一个磁贴应当清除筛选**，不需要去别处找「重置」。
14. 勾上「只看我提交的」→ 状态筛选应当被清除；
    反过来，mine 开着时点状态磁贴 → **「只看我提交的」应当自动取消勾选**。
    （这条是变异测试补出来的用例，坏掉的话计数会静默出错，界面不报任何异常。）
15. 筛选后按「后退」→ 应当回到上一组筛选条件，且列表数据跟着刷新。
16. 任务行点进去 → 跳到该项目的任务详情（`/projects/<id>/tasks?task=<key>`），
    复用 Phase 3 的深链接。
17. 列表里应当显示**项目代号**而不是裸的数字 id。
18. **权限重点**：用一个只属于 A 项目的账号登录，工作台**不应该**出现 B 项目的任何任务；
    手工把 URL 改成 `?project_id=<B的id>` **应当返回空列表，而不是 B 的任务**。
    这条我只能在代码层面证明（`if want in by_id:` 取交集，且有变异测试守着），
    **真实数据下必须你们实测**。
19. 任务超过 300 条时列表底部应当出现截断提示。
20. 用一个不属于任何项目的新账号登录 → 应当看到空状态，而不是报错或空白页。

**Phase 5（面包屑与导航语义）**

21. 依次打开某项目的 **任务/运行、成员管理、字段配置、模型管理、矩阵编辑、审计日志** 六个页面，
    每页顶部面包屑都应当是 `项目 / <代号> · <项目名> / <当前页>`。
    **重点看「矩阵编辑」和「审计日志」**——这两页改造前根本没有面包屑，
    显示的是 base 的默认值「测试平台」，用户无从判断自己在哪个项目里。
22. 面包屑中间那段项目名应当可点击，跳回该项目。
23. 在同一项目内连续切换上述页面 → 项目名应当**立刻**出现（走 sessionStorage 缓存），
    不应该每页都先闪一下 `项目 #<数字>`。
24. **跨项目切换**：从 A 项目的编辑页跳到 B 项目的成员页 →
    面包屑必须显示 B 的名字，**绝不能短暂显示 A 的名字**。
    （缓存按项目 id 存 key 就是为了这个；显示错项目名会让人误判自己在改哪个项目。）
25. 断网 / 用无权限账号访问 → 面包屑应当**安静地**停在 `项目 #<数字>`，
    不弹错误框、不报红。面包屑是导航装饰，不该盖在用户正在做的事情上面。
26. **「步骤明细」弹窗**：在「任务/运行」页和「矩阵编辑」页各打开一次，
    两边的字段、按钮、布局应当完全一致（现在是同一份模板）。
    特别确认**任务页的弹窗现在有「关闭」按钮**——改造前那页只能按 Esc 退出。
27. 弹窗里的任务状态徽标颜色应当正常（排队/运行中/成功/失败），
    与改造前一致。

### Phase 6（分页 / 静默截断）

28. 找一个**任务数超过 200** 的项目，打开「任务/运行」页：
    表格下方应出现「已加载最近 200 / 共 N 条」和「加载更多」。
    点一次，应变成 400，按钮仍在；一直点到 2000 会禁用并提示改用筛选。
    **任务数少于 200 的项目不应看到这一栏**（不是隐藏了个空条，是整栏不出现）。
29. 在已经「加载更多」到 400 之后，**等一个轮询周期**（约几秒）：
    列表不应缩回 200 条。这是本次最容易出错的地方——
    轮询若用回默认窗口，刚加载的行会无声消失。
30. 勾选几行 → 点「加载更多」→ 勾选状态应保留。
31. 开着筛选时，页脚应额外显示「（当前筛选显示 X 条）」，
    避免把「筛选后可见行数」和「已加载条数」当成同一个数。
32. 手动访问 `/lanmatrix/projects/<id>/tasks?limit=abc` 之类的坏参数，
    页面应正常加载（回落到默认 200），**不应 500**。
33. 「矩阵编辑」页的测试结果回填（每个 test_id 的最新判定）
    和步骤抽屉里的状态显示，应与改动前一致——
    尤其是**上次运行很久以前**的测试，不应变成「从未运行」。

### Phase 7（按钮方言收敛）

34. **这是本轮唯一的预期视觉变化，请重点看：**
    小按钮（表格行内的「编辑 / 删除 / 预览 / 步骤明细」、
    模型页 SBS 抽屉的「历史版本 / 重新载入 / 保存」、
    编辑器的「全选 / 清空」等，共 25 处）
    高度由「随内容自适应（约 25.5px）」变成**固定 30px**，
    即**长高约 4.5px**。这是有意为之（点击目标更大、与控制台其余按钮一致），
    请确认在表格行内不显得拥挤、不撑破行高。
35. 任一**禁用**按钮（如模型页未改动时的「保存」、
    导入对话框未选文件时的「确认导入」、
    DB 管理无插入权限时的「＋ 新增行」），
    鼠标悬停时**不应变色**。改动前 `.btn` 系按钮是会变色的（看起来可点），
    本轮一并修好。
36. 「导入 / 导出 / 队列测试」等主按钮外观应与改动前一致；
    主按钮现在多了一层很淡的投影（与全站其它主按钮统一）。
37. 用作按钮的链接（工作台页的「前往项目列表 / 查看全部」、
    左下角「退出登录」、任务详情的「下载结果」）
    悬停时**不应出现下划线**。

### Phase 8（状态徽章方言收敛）

> **先说结论：本轮没有改动任何状态颜色。**
> 我实测过 11 个 `lm-status-*` 与对应 `st-*` 的色值**逐字节相同**，
> 所以不需要你逐个比色。下面 4 条是**预期中的变化**，请确认它们符合预期。

38. **徽章多了一个圆点**：模型编辑器右上角的项目状态、
    步骤编辑器的运行状态、步骤明细对话框的状态，
    现在与「任务列表 / 工作台 / 管理台」一样，
    在文字左侧显示一个**同色圆点**。
    请确认圆点不与文字挤在一起、不撑破行高。
39. **英文变中文**：步骤编辑器/步骤明细对话框以前会直接显示
    `passed` / `running` / `failed` 等**英文原文**，现在显示
    「通过 / 运行中 / 失败」，与任务列表一致；
    以前显示的 `noTask` 现在显示「—」。
40. **深色主题**：切到深色主题后，
    「排队中 / 已取消 / 未运行」以及项目的「草稿 / 已归档」
    以前是**浅底浅字、几乎看不清**，现在应当可读。
    （这几个状态原来没有深色覆盖，是白拿到的修复。）
41. **措辞统一**：项目列表页的编辑对话框里，
    项目状态 `active` 以前写「活跃」，
    而同一页顶部的筛选器写「进行中」——现在统一为**「进行中」**。
42. 顺带检查：模型页右上角的**协作徽章**（「协作中 / 等待连接 / 未连接」，
    带呼吸动画）和 DB 管理页顶部的**管理员徽章**外观应与改动前一致；
    它们只是换了基类，样式没变。

### Phase 9 — 查询参数健壮性（**没有视觉变化**）

这一项肉眼看不出区别，要靠改 URL 验证。**原来是崩溃页，现在是中文提示。**

43. 打开任意项目的接口，手动把 URL 改成
    `/api/v1/projects/1/items?page=abc`。
    以前是 **500 崩溃页**，现在应返回 **400**，且提示里**点名是哪个参数**
    （类似「参数 page 必须是整数，收到：abc」）。
44. 同理试 `?filter=abc`（不是合法 JSON）、`?page=0`、`?page_size=100000`。
    都应是 400 + 中文说明，**不应**是 500，也**不应**悄悄按上限返回一页数据
    却让你以为拿到了全部。
45. 反向确认：**不带**任何参数的正常访问一切照旧。

### Phase 10 — 审计日志筛选

46. 进入某项目 → **审计日志**。顶部应出现筛选栏：
    关键字 / 操作 / 结果 / 操作人 / 起始日期 / 结束日期，以及「筛选」「重置」。
47. 「操作」和「操作人」两个下拉应**由真实数据填充**（不是写死的清单）。
    操作人应显示**姓名**而不是「操作人 3」这样的数字。
48. 选一个筛选条件 → 「筛选」。地址栏应同步出现该条件；
    **把这个 URL 复制到新标签页打开，筛选条件应当还在**（可以贴进工单）。
49. 按浏览器**后退**，应回到上一组筛选条件。
50. 故意选一个**必然没有结果**的组合（例如把起始日期设成明年）。
    空结果应显示「没有符合筛选条件的日志…」，
    **不能**显示「暂无日志」——后者会让人误以为压根没发生过操作。
51. 清空筛选后若日志超过 50 条，底部应出现「**加载更多**」，
    且计数行显示「已显示 N / 共 M 条」。以前是一次拉 200 条、不告诉你还有多少。
52. **筛选后再点「加载更多」**，应接着出第二页，不应跳过一页内容。
53. 日期筛选把「结束日期」设成**今天**，今天发生的操作应当**能被筛出来**
    （不能因为按零点算而整天消失）。

---

## 我没做的 / 已知限制

- **没有 PostgreSQL**，20 个数据库测试跑不了。**Phase 4 的 `me.py` 全是数据库查询，
  它的实际返回结果我一条都没验证过**——我只证明了代码层面的不变式
  （范围来自共享的成员过滤、`?project_id` 取交集、只认 `submitter_id`、
  有上限、LIKE 已转义），并用变异测试证明这些守卫确实会失败。
  **「查出来的数据对不对」必须你们接上真库实测**，尤其是上面第 18 条。
- **没有浏览器**，上面那些人工验证项我一条都没能实测。
- **`count_tasks()` 的真实查询没跑过**（同样是因为没有 PG）。
  我只证明了它和 `list_tasks` 共用同一条过滤链、且不带 limit；
  「共 N 条」这个数字对不对，必须接上真库看第 28 条。
- **`getStatus` 仍是「拉全量列表再客户端筛选」**。本轮把它显式钉在 2000 条，
  保证不比改动前（隐式 1000）更差，但**测试历史超过 2000 条的项目仍可能漏判**。
  正确解法是给每个测试单独的状态端点，属于独立改动，本轮未做。
- 前端 Univer/协同 bundle 未重新构建。
- ~~「步骤明细」弹窗的 HTML 重复了两份~~ → **Phase 5 已抽成 `_steps_dialog.html`**，
  缺失的关闭按钮也补上了。
- ~~`steps_editor.js` 仍在运行时写 `lm-badge lm-status-*` 旧词汇~~ →
  **Phase 8 已迁移完毕**（现已核实 `steps_editor.js` 中 `lm-badge` / `lm-status-*`
  零残留）。当时担心的「需人工比对四种状态颜色」也已消解：
  实测 11 个 `lm-status-*` 与对应 `st-*` **颜色逐字节相同**。
- **审计筛选的 SQL 没有真正执行过**（没有 PG）。我证明的是：每个筛选项恰好贡献
  一条形状正确的谓词、未启用的筛选不贡献任何谓词、LIKE 通配符已转义——
  并用 60 个变异证明这些断言确实会失败。**「筛出来的行对不对」必须接真库实测**
  （下面第 43–48 条）。
- i18n 框架、出站 Webhook、LDAP SSO 三项本轮明确不做，需要更大改造。

### Phase 11 · 提交前置检查条（清单 #15）

进入任一项目 → 任务与运行 → 展开提交面板，检查条在表单最顶部、提交按钮上方。

54. **什么都没做时**：应显示「模型」「测试用例」两行，其中「测试用例」为红色
    `!`（尚未选择测试用例文件夹）。若本项目没有注册过 .sil，「模型」也应是红色，
    且提示里**写明要找系统管理员**——这正是过去点了提交才会看到的那条。
55. 顶部摘要应显示「提交前置检查 · N 项待解决，现在提交会失败」。
    **注意不应出现「无法提交」**：模型在服务器上缺失这一档是能提交的，只是会跑失败。
56. 选择一个含 judge.py 的文件夹后，「测试用例」应立刻变成黄色（发现 N 个 test id，
    但一个都没勾选），勾选之后变绿并显示「已勾选 x / N」。**全程不需要点提交**。
57. **行的顺序**：红色在最上、黄色次之、绿色再次、灰色（你的角色）永远在最后。
58. 「你的角色」一行应是**灰色圆点 `·`**，不是绿勾——它是信息，不是检查项。
    如果它看起来像一个「通过」，说明样式错了。
59. **lib 不应该无故报警**：选一个 judge.py 只 `import os` 的文件夹、不选 lib 文件夹，
    「lib 依赖」应当是**绿色**（未引用需额外上传的模块），而不是「未选择 lib」。
60. 反过来，让 judge.py `import somethingmissing`，「lib 依赖」应变黄并**列出模块名**。
    此时**提交按钮仍然可用**——这是有意的，正则扫描不如服务端 AST 精确，
    宁可让误报廉价。
61. **深色主题**：切到深色再看一遍，四种颜色（红/黄/绿/灰）都应清晰可读。
    这是 Phase 8 踩过的坑。
62. **屏幕阅读器**（有条件的话）：检查条是 `role="status" aria-live="polite"`，
    勾选 test id 时应被朗读，且不应打断当前朗读。

**已知未验证**：以上全部为人工项，本环境无浏览器。`/tasks/preflight` 端点的真实
HTTP 行为（鉴权 403、载荷大小）同样未跑过，因为没有 PostgreSQL。

---

## Phase 12（#11）编辑器行操作

63. **不用右键也能操作**：打开编辑器，只用工具栏上的按钮完成「插入行 / 删除行 /
    上移 / 下移」。不选中任何行时，这四个按钮应当是**禁用**状态而不是点了没反应。
64. **多选移动保序**：选中第 2、5、7 行（不连续），点上移。三行应整体上移且
    **相对顺序不变**，不能被压成连续的一块。
65. **跨表选区被拒绝**：如果选区跨了 test / summary 两张表，应给出明确提示，
    而不是静默只移动其中一张表的行。
66. **`?` 帮助面板**：在编辑器里按 `?`，应弹出快捷键说明；再按 `?` 或 `Esc` 关闭。
    **在输入框里打 `?` 不应该弹面板**——这是最容易做错的一处。

## Phase 13（#12）审计日志

67. **看得出改了哪个字段**：改某条用例的一个字段，去审计页展开该条记录。应看到
    「字段名 / 旧值 / 新值」三列，而不是两大块 JSON。
68. **操作人是名字不是数字**：「操作人」列应显示用户名（或显示名），不是 `3`。
    离职用户（id 查不到）应回退显示原始 id，**不能变成空白**。
69. **CSV 导出带着筛选条件**：先按时间范围 + 操作类型筛选，再导出。CSV 的行数
    应与页面上看到的一致，而不是导出全量。用 Excel 打开，中文不应乱码。

## Phase 14（#10）回收站

70. **删除不再是终点**：删掉一条用例，去「回收站」，它应当在列表里。
71. **显示剩余保留期**：每条记录应显示「还有 N 天」和到期日期。
    **时间戳不能带 `+00:00` 尾巴**——这是本轮修掉的一个真 bug，回归时重点看。
72. **删除人对用例也要有**：删一条用例（不是字段），回收站的「删除人」列应显示
    你的名字。**这一列曾经对用例永远空白**，是本轮修掉的另一个真 bug。
    批量删除（多选后删）同样要显示。
73. **还原**：点「还原」，记录应回到原处，且**行的位置/顺序正确**，不是追加到末尾。
74. **彻底删除需要重打名称**：点「彻底删除」应要求手动输入该条目的名称才能确认，
    输错则按钮不可用。这是不可逆操作，确认成本必须高。
75. **权限分离**：用一个非管理员的项目成员账号登录，应当**能还原但不能彻底删除**
    ——「彻底删除」按钮不应出现（不是出现了点了报错）。
76. **删字段不会立刻抹掉数据**：删一个字段，去看用例表，该字段的值应仍在库里
    （从回收站还原后值应完整回来）。只有「彻底删除」或 30 天到期才真删。
77. **类型筛选**：用「用例 / 字段 / 任务」筛选，计数和列表应一致。
78. **到期清理**：把某条记录的 `deleted_at` 手工改到 31 天前，重启应用，
    它应当被自动清掉，且**启动日志里有一行记录**。清扫失败绝不能阻塞启动。

**已知未验证**：本环境无 PostgreSQL，`lm_test_items.deleted_by` 的迁移 DDL 与
`ix_*_deleted_at` 索引只在 SQLite 上跑通过；首次启动请确认日志里没有
「schema migration」相关的 warning。
