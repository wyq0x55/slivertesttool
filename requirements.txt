# Silver Test Platform - Python 3.10+ dependencies (offline / internal network)
Flask>=3.0,<4.0
SQLAlchemy>=2.0,<3.0
Flask-SQLAlchemy>=3.1,<4.0
huey>=2.5,<3.0
pydantic>=2.0,<3.0
waitress>=3.0,<4.0
python-dotenv>=1.0,<2.0
pytest>=8.0,<9.0
openpyxl>=3.1,<4.0            # Excel import/export (Test Matrix + LAN Test Matrix)
# --- PostgreSQL (required) ---
# The platform runs exclusively on PostgreSQL. psycopg2 backs both SQLAlchemy
# (postgresql+psycopg2://...) and peewee (the Huey SqlHuey queue storage).
psycopg2-binary>=2.9,<3.0
peewee>=3.17,<4.0            # PostgreSQL-backed Huey queue (huey.contrib.sql_huey)
# The installer auto-detects a matching wheel; otherwise it does a HYBRID
# install (vendored pure-python wheels + PyPI for pycrdt only).
#
# Versions pinned to the vendored/verified wheels.
pycrdt==0.14.1
pycrdt-websocket==0.16.4
pycrdt-store==0.1.5
sqlite-anyio==0.2.10
anyio==4.14.2
exceptiongroup==1.3.1
idna==3.18
typing_extensions==4.16.0
# uvicorn is the ASGI server for run_collab.py (already available in most envs).
uvicorn>=0.30
# REQUIRED: uvicorn needs a WebSocket protocol library to accept the upgrade
# handshake. WITHOUT it, uvicorn silently downgrades the WS request to a plain
# HTTP GET, the websocket-only ASGI app never sends a response, and the browser
# gets "500 Internal Server Error / ASGI callable returned without starting
# response". Pure-python (any OS/Python); comes from PyPI on the hybrid path.
# For a FULLY OFFLINE install, vendor its wheel into vendor/collab-wheels too:
#     pip download websockets   # then copy the .whl into vendor/collab-wheels/
websockets>=12,<14
# itsdangerous ships with Flask; app/collab/tokens.py imports it in both processes.
itsdangerous>=2.1
